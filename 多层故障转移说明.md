# DNSProxy 多层故障转移机制说明

版本: v2.2
日期: 2026-05-28

## 概述

DNSProxy v2.2 实现了完整的三层故障转移机制，确保DNS查询的高可用性。无论域名是否匹配到域名分组，都有完善的故障转移路径。

## 核心概念

### 1. upstream（全局兜底DNS）

**作用范围**: 对**所有域名**生效（包括匹配和未匹配域名分组的域名）

**生效场景**:
1. 当域名匹配到分组，但分组的主要DNS和备用DNS都失败时
2. 当域名未匹配任何分组，且default-upstream-group-id的主要DNS和备用DNS都失败时
3. 当没有设置default-upstream-group-id时，作为未匹配域名的默认DNS
4. 当default-upstream-group-id指定的分组不存在时

**配置示例**:
```yaml
upstream:
  - 8.8.8.8        # Google DNS（最终兜底）
  - 1.1.1.1        # Cloudflare DNS
```

### 2. default-upstream-group-id（未匹配域名的默认分组）

**作用范围**: **仅对未匹配任何域名分组的域名**生效

**用途**: 为未匹配的域名提供第1层和第2层的DNS服务器

**配置示例**:
```yaml
default-upstream-group-id: global-dns  # 引用上游分组ID
```

### 3. upstream-group-id（域名分组的上游分组）

**作用范围**: 对匹配到该域名分组的域名生效

**用途**: 为特定域名分组提供第1层和第2层的DNS服务器

**配置示例**:
```yaml
domain-groups:
  - name: china
    upstream-group-id: china-dns  # 引用上游分组ID
```

## 故障转移流程

### 场景1: 匹配到域名分组的查询

例如：查询 `baidu.com`，匹配到 `china` 域名分组

```
┌─────────────────────────────────────────────────────────────┐
│ 查询: baidu.com                                              │
│ 匹配: china 域名分组                                         │
└─────────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────────┐
│ 第1层: 分组主要上游 (china-dns 的 upstreams)                │
│ DNS服务器: [223.5.5.5, 119.29.29.29]                        │
│ 结果: 尝试所有主要DNS                                        │
└─────────────────────────────────────────────────────────────┘
                          ↓ 全部失败
┌─────────────────────────────────────────────────────────────┐
│ 第2层: 分组备用上游 (china-dns 的 fallback-upstreams)       │
│ DNS服务器: [114.114.114.114]                                 │
│ 结果: 尝试所有备用DNS                                        │
└─────────────────────────────────────────────────────────────┘
                          ↓ 全部失败
┌─────────────────────────────────────────────────────────────┐
│ 第3层: 全局upstream（最终兜底）                              │
│ DNS服务器: [8.8.8.8, 1.1.1.1]                               │
│ 结果: 尝试全局兜底DNS                                        │
└─────────────────────────────────────────────────────────────┘
                          ↓ 全部失败
                    ❌ 返回DNS查询失败
```

### 场景2: 未匹配任何域名分组的查询

例如：查询 `example.com`，未匹配任何域名分组

```
┌─────────────────────────────────────────────────────────────┐
│ 查询: example.com                                            │
│ 匹配: 无（未匹配任何域名分组）                               │
└─────────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────────┐
│ 第1层: 默认分组主要上游 (global-dns 的 upstreams)           │
│ DNS服务器: [8.8.8.8, 8.8.4.4]                               │
│ 结果: 尝试所有主要DNS                                        │
└─────────────────────────────────────────────────────────────┘
                          ↓ 全部失败
┌─────────────────────────────────────────────────────────────┐
│ 第2层: 默认分组备用上游 (global-dns 的 fallback-upstreams)  │
│ DNS服务器: [1.1.1.1, 1.0.0.1]                               │
│ 结果: 尝试所有备用DNS                                        │
└─────────────────────────────────────────────────────────────┘
                          ↓ 全部失败
┌─────────────────────────────────────────────────────────────┐
│ 第3层: 全局upstream（最终兜底）                              │
│ DNS服务器: [8.8.8.8, 1.1.1.1]                               │
│ 结果: 尝试全局兜底DNS                                        │
└─────────────────────────────────────────────────────────────┘
                          ↓ 全部失败
                    ❌ 返回DNS查询失败
```

## 完整配置示例

```yaml
# ============================================================================
# 上游DNS分组配置
# ============================================================================
upstream-groups:
  # 中国DNS分组（带主备）
  - id: china-dns
    description: 中国大陆DNS服务器
    upstreams:
      - 223.5.5.5      # 阿里DNS（主要）
      - 119.29.29.29   # 腾讯DNS（主要）
    fallback-upstreams:
      - 114.114.114.114  # 114DNS（备用）

  # 国际DNS分组（带主备）
  - id: global-dns
    description: 国际DNS服务器
    upstreams:
      - 8.8.8.8        # Google DNS（主要）
      - 8.8.4.4        # Google DNS备用（主要）
    fallback-upstreams:
      - 1.1.1.1        # Cloudflare DNS（备用）
      - 1.0.0.1        # Cloudflare DNS备用（备用）

# ============================================================================
# 未匹配域名的默认分组（仅对未匹配域名生效）
# ============================================================================
default-upstream-group-id: global-dns

# ============================================================================
# 全局兜底DNS（对所有域名生效）
# ============================================================================
# 作为最终兜底，在以下情况生效：
#   1. 没有设置default-upstream-group-id时，作为未匹配域名的默认DNS
#   2. default-upstream-group-id指定的分组不存在时
#   3. default-upstream-group-id的主要和备用DNS全部失效时
#   4. 域名分组的主要和备用DNS全部失效时（对所有域名生效）
upstream:
  - 8.8.8.8        # Google DNS（最终兜底）
  - 1.1.1.1        # Cloudflare DNS（最终兜底）

# ============================================================================
# 域名分组配置
# ============================================================================
domain-groups:
  # 中国域名 - 使用中国DNS
  - name: china
    enabled: true
    domain-file: china_domains.txt
    upstream-group-id: china-dns
    # 故障转移链：
    # 1. china-dns主要 [223.5.5.5, 119.29.29.29]
    # 2. china-dns备用 [114.114.114.114]
    # 3. 全局upstream [8.8.8.8, 1.1.1.1]

  # 广告域名 - 使用广告拦截DNS
  - name: ads
    enabled: true
    domain-file: ad_domains.txt
    upstream-group-id: adblock-dns
```

## 配置方式对比

### 方式1: 推荐配置（使用分组ID）

**优点**: 配置清晰、易于维护、支持复用

```yaml
upstream-groups:
  - id: china-dns
    upstreams: [223.5.5.5, 119.29.29.29]
    fallback-upstreams: [114.114.114.114]

default-upstream-group-id: global-dns
upstream: [8.8.8.8, 1.1.1.1]

domain-groups:
  - name: china
    upstream-group-id: china-dns
```

### 方式2: 直接配置（不使用分组ID）

**优点**: 配置简单、适合简单场景

```yaml
upstream: [8.8.8.8, 1.1.1.1]

domain-groups:
  - name: china
    upstream: [223.5.5.5, 119.29.29.29]           # 第1层
    fallback-upstream: [114.114.114.114]          # 第2层
    # 第3层使用全局upstream
```

### 方式3: 最小配置（仅全局DNS）

**优点**: 最简单、适合不需要分组的场景

```yaml
upstream:
  - 8.8.8.8
  - 1.1.1.1
```

## 关键区别说明

### upstream vs default-upstream-group-id

| 配置项 | 作用范围 | 用途 |
|--------|----------|------|
| `upstream` | **所有域名** | 最终兜底DNS，无论域名是否匹配分组 |
| `default-upstream-group-id` | **仅未匹配域名** | 为未匹配域名提供第1层和第2层DNS |

### 示例说明

假设配置如下：
```yaml
upstream-groups:
  - id: china-dns
    upstreams: [223.5.5.5]
    fallback-upstreams: [114.114.114.114]
  - id: global-dns
    upstreams: [8.8.8.8]
    fallback-upstreams: [1.1.1.1]

default-upstream-group-id: global-dns
upstream: [9.9.9.9]

domain-groups:
  - name: china
    domain-file: china_domains.txt
    upstream-group-id: china-dns
```

**查询 baidu.com（匹配china分组）**:
1. 尝试 223.5.5.5（china-dns主要）
2. 失败后尝试 114.114.114.114（china-dns备用）
3. 失败后尝试 9.9.9.9（全局upstream）✅ **注意：不使用global-dns**

**查询 example.com（未匹配任何分组）**:
1. 尝试 8.8.8.8（global-dns主要）
2. 失败后尝试 1.1.1.1（global-dns备用）
3. 失败后尝试 9.9.9.9（全局upstream）

## 常见问题

### Q1: 为什么需要三层故障转移？

**答**: 
- 第1层（主要DNS）：正常情况下使用，性能最优
- 第2层（备用DNS）：主要DNS失败时使用，提供第一层保护
- 第3层（全局DNS）：所有DNS都失败时的最后保障，确保服务可用

### Q2: upstream 和 default-upstream-group-id 可以同时配置吗？

**答**: 可以，而且推荐同时配置：
- `default-upstream-group-id`: 为未匹配域名提供第1层和第2层DNS
- `upstream`: 为所有域名提供第3层兜底DNS

### Q3: 如果不配置 default-upstream-group-id 会怎样？

**答**: 未匹配的域名会直接使用 `upstream` 作为第1层DNS（跳过第1层和第2层）

### Q4: 域名分组可以不配置 fallback-upstreams 吗？

**答**: 可以，如果不配置：
- 第1层：使用 upstreams
- 第2层：跳过（没有备用DNS）
- 第3层：使用全局 upstream

### Q5: 如何验证故障转移是否正常工作？

**答**: 
1. 启用详细日志：`verbose: true`
2. 配置一个无效的主要DNS，观察是否自动切换到备用DNS
3. 查看日志中的故障转移信息

## 性能建议

1. **主要DNS**: 选择延迟最低的DNS服务器
2. **备用DNS**: 选择可靠性高的DNS服务器
3. **全局DNS**: 选择全球可达的公共DNS服务器（如Google DNS、Cloudflare DNS）
4. **监控**: 定期检查各层DNS的使用情况和失败率

## 总结

DNSProxy v2.2 的三层故障转移机制提供了完善的高可用性保障：

✅ **对匹配域名**: 分组主要 → 分组备用 → 全局兜底
✅ **对未匹配域名**: 默认分组主要 → 默认分组备用 → 全局兜底
✅ **灵活配置**: 支持分组ID引用和直接配置两种方式
✅ **最终保障**: 全局upstream确保所有域名都有兜底方案

这种设计确保了即使在极端情况下（多个DNS服务器同时失败），DNS查询仍然有机会成功解析。
